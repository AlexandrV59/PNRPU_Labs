#pragma once

struct  fraction{
  double first;
  int second;
  void Init(double,int);//method for initialization field
  void Read();//Methof for read value of field
  void Show();//Method for output value of elements
  double Power();//Search for ^
};
